/// Shape of Modal Barrier
enum BarrierShape {
  normal,
  headerTrans,
}

/// FlexibleDropdownAnimation
///
enum AnimationType {
  scale,
  scaleY,
  scaleX,
  size, // available with full screen width
  fade,
  slide,
  rotate,
}
